document.addEventListener("DOMContentLoaded", function () {
  let e = document.querySelectorAll(".delete-button");
  e.forEach((e) => {
    e.addEventListener("click", function (e) {
      confirm("Are you sure you want to delete this word?") ||
        e.preventDefault();
    });
  });
});
